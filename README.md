## This repo consists of examples of different techniques to fine tune large language models. 

The first technique is Supervised Fine tuning, this uses examples to fine tune the LLM. The dataset used for fine tuning is smoltalk dataset from huggingFace.